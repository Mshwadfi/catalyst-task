import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.pexels.com', 'images.unsplash.com','test.catalystegy.com'], // Add your external domain here
  },
};

export default nextConfig;
